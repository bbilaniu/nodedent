---
"nodedent": patch
---

Show rejected case JSON import errors inside the import dialog, including when importing from the vault entry screen, with an accessible link to the input. Clear the error when the input changes.

Correct operative memo dependencies and stabilize Case Setup focus dependencies while introducing the lint gate.
