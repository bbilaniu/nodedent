---
"nodedent": patch
---

Give encrypted backup restore its own passphrase field and show actionable validation errors instead of silently disabling the restore action when its required input is missing.
