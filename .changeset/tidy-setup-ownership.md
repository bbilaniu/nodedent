---
"nodedent": patch
---

Keep endodontic and operative setup in their owning workflows instead of duplicating those sections in Case Setup & Status, while preserving workflow launch actions from Treatment plan.

Place the Sandbox data warning below the complete Import and Vault action row so it spans the protected-vault card width.
