---
"nodedent": minor
---

Improve chairside continuation and contextual requirements, add repeatable anesthesia and radiograph documentation with shared catalogue preferences, and add timestamped encrypted backups with safe new-encounter import, content-aware conflict review, protected replacement, and recoverable encrypted history. Make vault entry and recovery easier with direct case JSON file import, encrypted-vault import and download actions after unlock, and a persistent theme control on the vault lock screen. Clear paused canal status when treatment resumes so closure validation uses the canal's current derived treatment state.
